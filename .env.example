# Your Substack session cookie (required for paid content)
# You can find this in your browser's dev tools -> Application -> Cookies -> substack.sid
SUBSTACK_SID=

# Optional: override path to the Brave binary used by login.py
# Defaults are auto-detected per OS (macOS / Linux / Windows)
BRAVE_EXECUTABLE_PATH=
